mod filter;
/*
/// latex = "0.3.1"
pub use self::filter::*;

pub struct Method<'a>(Vec<Steps<'a>>);

pub struct Steps<'a> {

}

pub struct SubStep<'a> {
    description : &'a str,
    maths : &'a str
}

pub trait DescribeAddition {
    ///always should 'ignore' aka return None when GINORE AT IGNORE_ADDITION_AT is >= given filter 
    const IGNORE_ADDITION_AT : Filter;
    fn describe_add<'a>(&self,method : &mut Method<'a>,filter : &Filter);
}*/